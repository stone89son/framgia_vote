<% award.canlidates.each do |canlidate| %>
<div class="team-adward">
  <div class="team-img" style="background: url('<%= canlidate.banner_url %>')"></div>
  <div class="team-content">
    <p class="team-name"><%= canlidate.name %></p>
    <p class="team-subTitle"><span><%= canlidate.start %></span></p>
    <div class="team-text"><%= canlidate.description.try(:html_safe) %></div>
    <a class="seeMore" rel="team<%= canlidate.id %>">See more</a>
<% if current_user %>
  <% if current_user.canlidate_voted?(canlidate) %>
    <div class="vote active">voted</div>
  <% else %>
    <div class="vote"><%= link_to 'Vote now', votes_path(canlidate_id: canlidate.id), method: :post, remote: true, style: "color: white;" %></div>
  <% end %>
<% end %>
  </div>
  <div class="clr"></div>
 </div>
<% end %>
<div class="clr"></div>
<div class="icon-close"></div>
